apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - networkpolicy.yaml
  - api-gateway.yaml
  - work-orders.yaml
  - notifications.yaml
  - properties.yaml
  - audit.yaml
# secret.example.yaml is intentionally NOT applied — create propflow-secrets
# yourself (see the file header) before applying this kustomization.
